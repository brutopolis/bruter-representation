load {std};
load {list};
load {io};
load {type};

# [&return &88]
    name: ctx;

# [&0 &1]
    name: cmd;

print.int (list.call ctx cmd);

list.unshift [] name: abc print.int;
list.push abc 99;
print.list abc;
// {we need to type this list as baked to avoid error};
[abc] 
    name: abc2 
    type: 5;

: 4;