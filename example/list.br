load {std};
load {list};
load {io};

# (list_init) @cmd;
# (list_init 1) @ctx;
# {lets add some functions to the list};

list_push ctx [return];
list_push ctx [88];
list_push cmd [0];
list_push cmd [1];

print_int (list_call ctx cmd);

return 4;